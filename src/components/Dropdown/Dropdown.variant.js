import { css } from 'styled-components';

const SIZES = {
  sm: css`
    --dropdown-width: 123px;
    --dropdown-height: 40px;
    --dropdown-padding: 12px 20px;
  `,
  md: css`
    --dropdown-width: 100%;
    --dropdown-height: 60px;
    --dropdown-padding: 20px;
  `
};

export default SIZES;
